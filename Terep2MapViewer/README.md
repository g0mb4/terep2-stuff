# Terep2MapViewer

Utility that can visualize Terep2 maps in a browser.
It is planned to be used in https://terep2.wiki.gg/wiki/Index_of_maps.

## Local usage

Start local webserver:
```bash
python -m http.server
```

Modify the parameters of `initTerep2MapViewer()` in *index.html*.
